# --- Core framework ---
fastapi==0.116.1
uvicorn[standard]==0.35.0

# --- Settings & validation ---
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.1.1

# --- Database (Supabase) ---
supabase==2.17.0

# --- Security / password tooling ---
# Password generation uses Python's built-in `secrets` module (no package needed).
argon2-cffi==25.1.0        # master-password hashing (Argon2id)
cryptography==45.0.5       # AES-GCM encryption for stored secrets
zxcvbn==4.5.0              # password strength estimation

# --- HTTP client (used by supabase, pinned for stability) ---
httpx==0.28.1

ruff>=0.16.1

pyjwt==2.10.1